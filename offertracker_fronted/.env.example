# API 配置
# 开发环境可以使用默认值，生产环境需要配置实际域名
VITE_API_BASE_URL=https://your-backend-domain.workers.dev
VITE_S3_BASE_URL=https://your-s3-domain.com

# Gemini AI 配置（可选）
# 如果需要使用AI功能，请配置Gemini API密钥
GEMINI_API_KEY=your_gemini_api_key_here

# 部署说明：
# 1. 复制此文件为 .env.local (开发环境) 或 .env.production (生产环境)
# 2. 根据实际环境修改变量值
# 3. 不要将包含真实密钥的文件提交到版本控制